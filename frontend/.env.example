# Local development — leave VITE_API_URL unset; Vite proxies /api to localhost:3001

# Production — set in Vercel (see .env.production.example):
# VITE_API_URL=https://dispatchledger.onrender.com/api
